# Email Service Configuration
# Copy these variables to your .env file and update with your actual values

# Email Service Provider (gmail, outlook, etc.)
EMAIL_SERVICE=gmail

# SMTP Configuration (for Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Email Authentication
# For Gmail: Use your email and an App Password (not your regular password)
# To create App Password: Google Account Settings > Security > 2-Step Verification > App passwords
EMAIL_USER="eatwiser19@gmail.com"
EMAIL_PASSWORD="iuai gnsp gshs zeoa"


# Admin Email (where payment notifications will be sent)
ADMIN_EMAIL=rithesh197rao@gmail.com

# Client URL (for email links)
CLIENT_URL=http://localhost:3000

# PhonePe Configuration (if not already set)
PHONEPE_MERCHANT_ID=your-merchant-id
PHONEPE_MERCHANT_KEY=your-merchant-key
PHONEPE_SALT_KEY=your-salt-key
PHONEPE_SALT_INDEX=1
PHONEPE_ENVIRONMENT=UAT
PHONEPE_REDIRECT_URL=http://localhost:3000/payment/success
PHONEPE_CALLBACK_URL=http://localhost:5003/api/payments/callback

# Example Email Configuration for Different Providers:

# Gmail Configuration:
# EMAIL_SERVICE=gmail
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587

# Outlook/Hotmail Configuration:
# EMAIL_SERVICE=outlook
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587

# Yahoo Configuration:
# EMAIL_SERVICE=yahoo
# EMAIL_HOST=smtp.mail.yahoo.com
# EMAIL_PORT=587

# Custom SMTP Configuration:
# EMAIL_SERVICE=custom
# EMAIL_HOST=your-smtp-server.com
# EMAIL_PORT=587
# EMAIL_SECURE=false

# Notes:
# 1. Make sure to enable "Less secure app access" or use App passwords for Gmail
# 2. For production, use environment-specific email settings
# 3. Consider using email services like SendGrid, Mailgun for production
# 4. Test email configuration using the /api/payments/test-email endpoint